services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:4173"
      - "5173:5173"
    volumes:
      - .:/app
    command:
      - "sleep"
      - "infinity"
